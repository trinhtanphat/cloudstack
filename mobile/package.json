{
  "name": "vnso-cloudstack-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "VNSO CloudStack mobile client — Capacitor wrapper around the management UI (PWA + Android + iOS).",
  "scripts": {
    "build:web": "node scripts/build-web.js",
    "sync": "cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "run:android": "cap run android",
    "run:ios": "cap run ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor/preferences": "^6.0.2",
    "@capacitor/push-notifications": "^6.0.2",
    "@capacitor/status-bar": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
